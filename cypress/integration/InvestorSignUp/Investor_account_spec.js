import { InvestorFlowProcess } from '../../support/investorSignup/investorFlow';
describe('Account Creation', () => {
  beforeEach(() => {
    InvestorFlowProcess();
  });
  it('succesfully go to signUp screen', () => {
    
  });
}); 